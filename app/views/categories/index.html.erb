<div class="page-content">
  <h1>Categories overview</h1>

 <div class="pagination">
    <span class="pagination"><%= will_paginate @categories, :container => false %></span>
  </div>

  <div class="container">
    <% @categories.each do |category| %>
    <div class="row justify-content-md-center">
      <div class="card text-center col-6 mt-4 article__card mb-5 bg-white rounded">

        <div class="card-body">
            <h5 class="card-title mb-3"><%= link_to category.name, category_path(category), :title => "see category details" %></h5>
            </div>

        <div class="card-footer text-muted">

        </div>

      </div>
  </div>
    <% end %>
  </div>

  <div class="pagination">
      <span class="pagination"><%= will_paginate @categories, :container => false %></span>
  </div>
</div>